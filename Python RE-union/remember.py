# divmod(), a special python arithmeic function that does floor division and modulus at the same time.
# dir(collection), to list the different methods and attributes available to a collection
# help(collection), for description of each of the methods
# end=" ", is a keyword argument
# print("1", "2", "3", "4", "5", sep="_"), sep="" too, is a keyword argument
# for number in reversed(numbers): for iterating the values in reversed form
# dundar = double underscore
# from file1 import * (*, here means everything)
# != is not equals to
# a = (), error. a = set(), right way
# print('') if condition else print(''), tis called turnary operator
# if a child shares a similar method as a parent, we'll use the child's version not the parent's
# self._parameter, this means that the specific parameter is not to be accessed externally, it's protected
# import os, os means operating system. Provides a way to intract with os.